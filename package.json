{
  "name": "npm-react-use-object",
  "version": "0.0.1",
  "type": "module",
  "description": "A temporary test package. Provides a hook for fetching information on a single object from https://restful-api.dev/",
  "main": "index.js",
  "scripts": {
    "build:dev": "cross-env NODE_ENV=development rollup -c",
    "build": "cross-env NODE_ENV=production rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "npm",
    "react",
    "hooks"
  ],
  "author": "Shlomo Moller",
  "license": "MIT",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "cross-env": "^7.0.3",
    "rollup": "^4.18.0"
  }
}
